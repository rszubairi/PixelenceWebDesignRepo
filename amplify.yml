version: 1
applications:
  - appRoot: pixelence-mri-system
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: .
        files:
          - '.next/**/*'
          - 'public/**/*'
          - 'package.json'
          - 'next.config.js'
      cache:
        paths:
          - node_modules/**/*
          - .next/cache/**/*
